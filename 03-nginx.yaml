- name: install nginx and run
  hosts: web
  become: yes
  tasks: 
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present
  
  - name: run nginx
    ansible.buildin.service:
      name: nginx
      state: started
      enable: yes